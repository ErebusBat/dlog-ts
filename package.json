{
  "name": "dlog",
  "version": "0.1.0",
  "module": "src/index.ts",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^24.0.0",
    "@types/strftime": "^0.9.8",
    "prettier": "^3.9.6",
    "tsx": "^4.23.0",
    "typescript": "^5.9.0"
  },
  "private": true,
  "scripts": {
    "repl": "tsx",
    "test": "bun test --concurrent --parallel",
    "test:watch": "bun test --watch --dots --only-failures --concurrent",
    "check": "just check",
    "tsc": "just tsc",
    "tsc:watch": "just tsc-watch",
    "format": "prettier --write ."
  },
  "type": "module",
  "dependencies": {
    "strftime": "^0.10.3",
    "zod": "^4.4.3"
  }
}
